import "./style.scss";
import a from "./a";
const x = 1;

if (x) {
    for (let i = 0; i < 100; i++) {
        while (true) require("./b");
        do {
            i++;
        } while (require("m1/a")());
    }
}
